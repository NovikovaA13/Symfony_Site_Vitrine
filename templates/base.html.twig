<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{% block title %}{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('build/css/font-awesome.css') }}">
        <link rel="stylesheet" href="{{ asset('build/owl-carousel/assets/owl.carousel.min.css') }}">
        <link rel="stylesheet" href="{{ asset('build/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('build/css/style.css') }}">
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light bg-transparent" id="gtco-main-nav"
     xmlns="http://www.w3.org/1999/html">
    <div class="container"><a class="navbar-brand">Solution</a>
        <button class="navbar-toggler" data-target="#my-nav" onclick="myFunction(this)" data-toggle="collapse"><span
                    class="bar1"></span> <span class="bar2"></span> <span class="bar3"></span></button>
        <div id="my-nav" class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('index')}}">Home</a></li>
                <li class="nav-item"><a href="{{ path('all_services') }}" class="nav-link" href="#services">Services</a></li>
                <li class="nav-item"><a href="{{ path('about') }}" class="nav-link" href="#about">About</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('index')}}#news">News</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('guestbook') }}">Guestbook</a></li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                {% if not is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('login') }}" class="btn btn-outline-dark my-2 my-sm-0 mr-3 text-uppercase">login</a>
                {% else %}
                    <a href="{{ path('admin_main') }}" class="btn btn-outline-dark my-2 my-sm-0 mr-3 text-uppercase">Admin menu</a>
                    <a href="{{ path('logout') }}" class="btn btn-outline-dark my-2 my-sm-0 mr-3 text-uppercase">logout</a>
                {% endif %}
            </form>
        </div>
    </div>
</nav>
{% block body %}{% endblock %}
</body>
    {% block javascripts %}
        <script src="{{ asset('build/js/jquery-3.3.1.slim.min.js') }}"></script>
        <script src="{{ asset('build/js/popper.min.js') }}"></script>
        <script src="{{ asset('build/js/bootstrap.min.js') }}"></script>
        <script src="{{ asset('build/owl-carousel/owl.carousel.min.js') }}"></script>
        <script src="{{ asset('build/js/main.js') }}"></script>
    {% endblock %}
</html>
