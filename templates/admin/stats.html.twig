{% extends 'admin.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}

<div class="container">
    <h1 class="mt-2 mb-4">Statistiques</h1>
    <div class="list-group mb-4">
        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nom</th>
                <th scope="col">Quantité</th>
                <th scope="col">Modifier</th>
                <th scope="col">Supprimer</th>
            </tr>
            </thead>
        {% for stat in stats %}
                <tr class="">
                    <td >{{ stat.id }}</td>
                    <td>{{ stat.title }}</td>
                    <td>{{ stat.quantity }}</td>
                    <td><a href="#" class="link-warning">Modifier</a></td>
                    <td><a href="#" class="link-danger">Supprimer</a></td>
                </tr>
        {% endfor %}
        </table>
    </div>
</div>
{% endblock %}
